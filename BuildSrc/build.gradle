apply plugin: 'groovy'

dependencies {
    compile gradleApi()
    compile localGroovy()
    compile 'com.android.tools.build:gradle:2.3.2'
    compile 'org.javassist:javassist:3.20.0-GA'
}

repositories {
    jcenter()
}